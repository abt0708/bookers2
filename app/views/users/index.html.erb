<h1>ここはユーザ一覧ページになる予定</h1>
<main>
  <div class="container px-5 px-sm-0">
    <div class="row">
      <!-- 左カラム：ログインユーザー情報と新規投稿 -->
      <div class="col-md-3">
        <%= render 'users/user', user: current_user, book: Book.new %>
      </div>

      <!-- 右カラム：ユーザー一覧 -->
      <div class="col-md-8 offset-md-1">
        <h2>Users</h2>
        <table class="table">
          <tr>
            <th>image</th>
            <th>Name</th>
            <th></th>
          </tr>
          <% @users.each do |user| %>
            <tr>
              <td>
                <% if user.profile_image.attached? %>
                  <%= image_tag user.profile_image, size: "80x80" %>
                <% else %>
                  <%= image_tag 'no_image', size: "80x80" %>
                <% end %>
              </td>
              <td><%= link_to user.name, user_path(user) %></td>
              <td><%= link_to 'show', user_path(user) %></td>
            </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
</main>
